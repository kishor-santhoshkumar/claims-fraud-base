import { Settings as SettingsIcon } from 'lucide-react'
import EmptyState from '../components/EmptyState'

export default function Settings() {
  return <EmptyState icon={SettingsIcon} label="Settings — coming soon" />
}
